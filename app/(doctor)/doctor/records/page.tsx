import { FileText } from "lucide-react";
import { PlaceholderPage } from "../_placeholder";

export default function RecordsPage() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Medical Records"
      description="Upload reports, review patient files, and manage all medical documentation in one secure place."
    />
  );
}
