import { Calendar } from "lucide-react";
import { PlaceholderPage } from "../_placeholder";

export default function AppointmentsPage() {
  return (
    <PlaceholderPage
      icon={Calendar}
      title="Appointments"
      description="View your schedule, approve requests, and manage appointment status — scheduled, completed, cancelled, or no-show."
    />
  );
}
