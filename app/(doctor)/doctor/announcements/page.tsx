import { Megaphone } from "lucide-react";
import { PlaceholderPage } from "../_placeholder";

export default function AnnouncementsPage() {
  return (
    <PlaceholderPage
      icon={Megaphone}
      title="Announcements"
      description="Send clinic notices to patients — holiday closures, new services, or updates for Pro members."
    />
  );
}
